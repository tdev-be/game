{% extends 'base.html.twig' %}
{% block body %}
    Hello
    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
        {{ app.user.username }}
    {% else %}
        World
    {% endif %}
    !<br/>
{% endblock %}